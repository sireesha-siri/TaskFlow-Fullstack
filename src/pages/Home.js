import React from "react";
import TodoList from "../components/TodoList";

const Home = () => {
    return (
        <div className="todos-bg-container">
            <TodoList />
        </div>
    );
};

export default Home;
